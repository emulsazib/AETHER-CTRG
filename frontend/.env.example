# AETHER Frontend environment template (copy to `.env`).
# Vite only exposes vars prefixed with VITE_ to the client.

# Base URL of the Node API Gateway.
VITE_API_BASE=http://localhost:4000/api/v1

# How often (ms) the UI polls a running analysis job for status.
VITE_POLL_INTERVAL_MS=2000
