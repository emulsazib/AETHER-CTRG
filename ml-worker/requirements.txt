# AETHER AI/ML Worker dependencies (mock pipeline — lightweight by design).
# Loose lower-bound pins so prebuilt wheels resolve across Python 3.10–3.14.
# When you swap in REAL models, add e.g. torch, transformers, faiss-cpu,
# umap-learn, shap, lime, pillow here.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
python-dotenv>=1.0

# External AI integration: any OpenAI-compatible gateway (OpenAI, vLLM, LiteLLM,
# Anthropic OpenAI-compat, self-hosted). Configured via AI_API_KEY/AI_BASE_URL/AI_MODEL.
openai>=1.40

# OpenCTI client — pushes extracted IoCs/TTPs as STIX2 for automated attribution.
pycti>=6.0.0

# External OSINT threat-intel (best-effort, key-gated). Lightweight + torch-free,
# so they live in the base image: AbuseIPDB (REST), AlienVault OTX, VirusTotal v3.
requests>=2.31
OTXv2>=1.5.12
vt-py>=0.18

